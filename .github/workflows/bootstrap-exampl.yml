name: ABC Bootstrap Example

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create files
        run: |
          mkdir -p lessons/grade1
          cat > index.html <<EOF
<!DOCTYPE html>
<html><body>
Hello World!
</body></html>
EOF
          cat > lessons/grade1/lesson1.json <<EOF
{
  "title": "ABC"
}
EOF
